import { useState } from 'react';
import styles from './MultipleChoice.module.scss';
import { FieldProps } from '../shared.interfaces';

export function MultipleChoiceField(props: FieldProps): JSX.Element {
    return (
        <div>

        </div>
    );
}
